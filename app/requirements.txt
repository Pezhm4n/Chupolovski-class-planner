# Core PyQt5 requirements
PyQt5>=5.15.0

# Network & REST API client
requests>=2.28.0

# Web scraping requirements
beautifulsoup4>=4.11.0
python-dotenv>=0.21.0

# OS keychain storage for cloud JWT tokens (TokenManager;
# falls back to in-memory storage when unavailable)
keyring>=23.0.0

# Credential encryption for local Golestan credentials
# (Fernet cipher in app/core/credentials.py)
cryptography>=3.4.8

# Captcha solving — heavy optional dependency.
# Imported lazily by app/captcha_solver/predict.py; the app
# runs fine without it (captcha is solved server-side).
tensorflow>=2.10.0
numpy>=1.21.0

# Optional: Additional fonts for better Persian support
# Install system fonts like Tahoma, B Nazanin, etc. manually if needed

# For development and testing
pytest>=7.0.0
pytest-qt>=4.0.0
